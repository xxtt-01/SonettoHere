# SonettoHere v2.1 — 项目结构总览

## 项目定位

SonettoHere 是一个基于 **LangChain + LangGraph** 构建的 **ReAct AI Agent** 教学项目。它以"温暖体贴的大姐姐 Sonetto"为人设，通过 30 个 Skill 覆盖任务管理、地图服务、网络查询、文件操作、开发工具等 10 个功能领域。项目同时支持 CLI、QQ Bot 和 Web UI 三入口。

该项目的技术骨架直接可作为 LangChain ReAct Agent 的入门教案——代码量适中，架构分层清晰，每一层都对应 LangChain 生态中的一个核心概念。

---

## 技术栈

| 组件 | 技术选型 | 作用 |
|------|----------|------|
| Agent 框架 | `langgraph >= 0.2.0` | 构建 ReAct Agent 状态图 |
| LLM 后端 | `langchain-openai`（DeepSeek Chat） | OpenAI 兼容的 tool calling |
| 状态管理 | LangGraph `MemorySaver` + 自建 `ShortTermMemory` | 会话状态持久化 + token 裁剪 |
| 配置管理 | `pydantic-settings` | 从 `.env` 加载 API Key |
| 工具定义 | LangChain `BaseTool` + Pydantic `BaseModel` | 30 个 Skill 的参数校验与执行 |
| 终端输出 | `colorama` + LangChain `BaseCallbackHandler` | 彩色流式输出 |
| Web 前端 | `Vue 3` + `TypeScript` + `Vite` | 现代化聊天界面 |
| Web 后端 | `FastAPI` + `uvicorn` + WebSocket | REST API + 流式 Agent 推送 |

---

## 目录结构

```
SonettoHere/
├── main.py                      # 入口选择器（cli / qqbot / web）
├── pyproject.toml               # 项目元数据与依赖声明
├── requirements.txt             # pip 依赖清单
├── .env.example                 # 环境变量模板
│
├── config/                      # ★ 配置层
│   ├── settings.py              #   Pydantic Settings，从 .env 加载所有 API Key
│   └── personas/                #   人设文件（Markdown 纯文本）
│       ├── AGENTS.md            #     行为规则 → 注入系统提示词
│       ├── SOUL.md              #     性格设定 → "Sonetto 大姐姐"
│       └── MEMORY.md            #     固定记忆 → 用户背景信息
│
├── agent/                       # ★ Agent 核心层
│   ├── graph.py                 #   LangGraph create_react_agent 封装
│   ├── state.py                 #   AgentState TypedDict 定义
│   └── prompts.py               #   系统提示词组装（带启动缓存）
│
├── skills/                      # ★ 工具层 — 30 个 Skill
│   ├── base.py                  #   SkillBase 基类 + SharedAPIClient
│   ├── __init__.py              #   get_all_skills() 集中注册
│   ├── todo/                    #   Todoist 任务管理（8 个）
│   ├── map/                     #   高德地图服务（5 个）
│   ├── network/                 #   网络服务（4 个）
│   ├── system/                  #   系统工具（2 个）
│   ├── files/                   #   文件操作（3 个）
│   ├── development/             #   开发工具（4 个）
│   ├── task/                    #   任务追踪（1 个）
│   ├── interaction/             #   用户交互（1 个）
│   └── entertainment/           #   娱乐（2 个）
│
├── memory/                      # ★ 记忆层
│   ├── short_term.py            #   短期记忆 — InMemoryChatMessageHistory + token 裁剪
│   ├── long_term.py             #   长期记忆 — 关键词+置信度+时间衰减评分检索
│   ├── extractor.py             #   回合摘要提取 — LLM 驱动的对话关键信息抽取
│   └── preference.py            #   用户偏好画像 — 从长期记忆中聚合稳定偏好
│
├── api/                         # ★ Web API 层（v2.1 新增）
│   ├── server.py                #   FastAPI 应用工厂 — 生命周期、CORS、静态文件
│   ├── session_manager.py       #   会话管理器 — 内存存储 + TTL 过期
│   ├── dependencies.py          #   共享资源 — LLM、提示词、工具懒加载单例
│   ├── routes/                  #   路由端点
│   │   ├── chat.py              #     WebSocket — 流式 Agent 对话
│   │   ├── sessions.py          #     REST — 会话 CRUD
│   │   └── memory.py            #     REST — 长期记忆叙事
│   └── callbacks/               #   WebSocket 回调
│       └── websocket_callback.py #   LangChain → JSON 事件桥接
│
├── web/                         # ★ Web 前端（v2.1 新增）
│   ├── index.html               #   浏览器入口
│   ├── package.json             #   前端依赖与脚本
│   ├── vite.config.ts           #   Vite 构建配置 + 开发代理
│   ├── tsconfig.json            #   TypeScript 配置
│   └── src/                     #   源码（14 个文件）
│       ├── main.ts              #     Vue 应用入口
│       ├── App.vue              #     根组件
│       ├── types/index.ts       #     TypeScript 类型定义
│       ├── api/index.ts         #     REST API 客户端
│       ├── router/index.ts      #     Vue Router 路由
│       ├── composables/         #     组合式 API
│       │   ├── useChat.ts       #       WebSocket 聊天管理
│       │   └── useSession.ts    #       会话状态管理
│       ├── views/               #     页面组件
│       │   ├── ChatView.vue     #       聊天页面
│       │   └── MemoryView.vue   #       记忆页面
│       └── components/          #     可复用组件 (8 个)
│
├── callbacks/                   # ★ 回调层
│   └── printer.py               #   LangChain Callback Handler — 彩色终端输出
│
├── clients/                     # ★ 入口层
│   ├── cli.py                   #   CLI 异步对话界面 — 事件流驱动
│   └── qqbot.py                 #   QQ Bot 适配器（骨架）
│
└── extracted/                   # ★ 持久化数据
    ├── extracted_history.json   #   长期记忆历史记录
    └── user_preference_profile.json  # 用户稳定偏好画像
```

---

## 架构分层

项目严格遵循 **关注点分离**，从底层到上层共六层：

```
┌─────────────────────────────────────────┐
│  clients/         入口层（CLI / QQ Bot）   │
│  api/             Web API 层（REST + WS） │
│  web/             Web 前端（Vue 3 SPA）   │
├─────────────────────────────────────────┤
│  callbacks/       回调层（终端 / WebSocket）│
├─────────────────────────────────────────┤
│  agent/           Agent 核心层            │
│  (graph + state + prompts)              │
├─────────────────────────────────────────┤
│  skills/          工具层（30 个 Skill）     │
├─────────────────────────────────────────┤
│  memory/          记忆层（短/长期记忆）      │
├─────────────────────────────────────────┤
│  config/          配置层（Settings + 人设） │
└─────────────────────────────────────────┘
```

---

## 运行时数据流

一次完整的对话回合经历了以下路径：

```
1. 用户输入 "明天北京天气怎么样？"
       │
2. CLI (_stream_events) 包装为 {"messages": [{"role":"user", "content":"..."}]}
       │
3. 注入增强上下文（长期记忆检索 + 偏好画像）
       │
4. LangGraph StateGraph 将消息送入 LLM
       │
5. LLM 决策 → tool_call: get_current_weather(city="北京", forecast=true)
       │
6. WeatherSkill._run() 执行 → 调用 UAPI → 返回天气数据 JSON
       │
7. LLM 接收 tool 响应 → 生成自然语言回答
       │
8. PrinterCallback 流式输出思考过程 + Skill 调用 + 最终回答
       │
9. 回合结束 → Extractor 提取摘要 → 写入长期记忆
```

---

## 关键设计决策

**1. 为什么用 `create_react_agent` 而非手动构建？**

LangGraph 的 `create_react_agent` 封装了 ReAct 循环的样板代码：LLM 调用 → 解析 tool_call → 执行工具 → 将结果送回 LLM → 循环直到 LLM 输出最终回答。手动实现需要处理消息合并、递归限制、异常恢复等细节。对于教学场景，使用预构建函数可以聚焦于理解 ReAct 原理而非工程细节。

**2. 为什么用 `get_doc=true` 两步调用模式？**

大多数 Skill 的第一个参数是 `get_doc: bool = False`。当 LLM 首次遇到某个 Skill 时，它先传 `get_doc=true` 获取同目录下 `SKILL.md` 的领域知识文档，再带真实参数执行。这样做的好处：将领域知识从代码中分离出来（Markdown 便于维护），同时避免了将全部 30 个 Skill 的文档塞入系统提示词（超出上下文限制）。

**3. 为什么自建记忆系统而不依赖 LangChain 内置方案？**

LangChain 提供了 `ConversationBufferMemory`、`ConversationSummaryMemory` 等内置方案，但本项目需要更精细的控制：基于 token 计数的自动裁剪（短期记忆）、关键词+置信度+时间衰减的多维度检索评分（长期记忆）、LLM 驱动的结构化信息提取。这些需求超出了内置方案的覆盖范围。

**4. 为什么用 Pydantic `BaseSettings` 管理配置？**

`BaseSettings` 自动从环境变量和 `.env` 文件加载配置，类型安全，支持默认值。相比 `os.getenv` 散落在各处，集中管理所有 API Key 更易于维护和审计。

---

## 阅读路线

按以下顺序阅读各模块文档，逐步建立对 LangChain ReAct Agent 的完整理解：

| 序号 | 文档 | 涉及概念 |
|------|------|----------|
| 1 | [ReAct Agent 核心原理](01-ReAct-Agent核心原理.md) | ReAct 范式、Thought-Action-Observation 循环 |
| 2 | [LangGraph 状态图与状态管理](02-LangGraph状态图与状态管理.md) | StateGraph、AgentState、MemorySaver、递归限制 |
| 3 | [Tool 与 Skill 系统](03-Tool与Skill系统.md) | BaseTool、Pydantic args_schema、两步调用模式 |
| 4 | [记忆系统](04-记忆系统.md) | 短期记忆裁剪、长期记忆检索、偏好画像 |
| 5 | [提示词与人设系统](05-提示词与人设系统.md) | 系统提示词组装、personas 设计 |
| 6 | [客户端与回调系统](06-客户端与回调系统.md) | 异步事件流、CallbackHandler、CLI 交互 |
| **7** | **[Web 前端系统总览](07-Web前端系统总览.md)** | **Vue 3 SPA 架构、前后端分离、通信协议一览** |
| **8** | **[Vue 3 组件树与响应式数据流](08-Vue3组件树与响应式数据流.md)** | **SFC 结构、props/emits、ref/reactive、组件嵌套** |
| **9** | **[前端路由与组合式 API](09-前端路由与组合式API.md)** | **Vue Router、Composables 模式、useChat/useSession** |
| **10** | **[REST API 与前后端数据交互](10-REST与前后端数据交互.md)** | **fetch 封装、类型安全、会话管理流程** |
| **11** | **[WebSocket 实时流式通信](11-WebSocket实时流式通信.md)** | **WebSocket 协议、事件驱动、流式 token 渲染、取消机制** |
| **12** | **[从开发到生产：前端工程化](12-从开发到生产：前端工程化.md)** | **Vite 代理与构建、静态文件服务、CSS 变量主题** |
